# Godot 4+ specific ignores
.godot/
/android/

# Resíduo do teste de clone (pode ser apagado manualmente)
/lightDark/

# Terrain3D (plugin 3D, nao usado neste projeto 2D) - residuo travado pelo Windows
/addons/terrain_3d/

# Packs originais do craftpix (fonte, com PSD/ASEPRITE) — só o que foi
# extraído/processado pra assets/ entra no jogo. Ficam aqui como arquivo,
# fora do controle de versão (são grandes e não são usados em runtime).
/raw_assets/

